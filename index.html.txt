<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Currency Converter (Offline)</title>
  <style>
    body { font-family: Arial, sans-serif; text-align: center; margin: 50px; background: #f4f4f9; }
    select, input, button { padding: 10px; margin: 10px; font-size: 16px; border-radius: 6px; border: 1px solid #ccc; }
    .card { display: inline-block; padding: 20px; border-radius: 12px; background: #fff; box-shadow: 0 0 12px rgba(0,0,0,0.1); }
    button { background: #4CAF50; color: white; border: none; cursor: pointer; }
    button:hover { background: #45a049; }
  </style>
</head>
<body>
  <div class="card">
    <h2>🌍 Currency Converter (Offline - 120+)</h2>
    <input type="number" id="amount" placeholder="Enter Amount" />
    <br>
    <select id="from"></select>
    <select id="to"></select>
    <br>
    <button onclick="convert()">Convert</button>
    <h3 id="result">Converted Amount: </h3>
  </div>

  <script>
    let rates = {};

    // Load rates from JSON file
    fetch("currencies.json")
      .then(response => response.json())
      .then(data => {
        rates = data;

        const fromSelect = document.getElementById("from");
        const toSelect = document.getElementById("to");

        Object.keys(rates).forEach(currency => {
          fromSelect.innerHTML += `<option value="${currency}">${currency}</option>`;
          toSelect.innerHTML += `<option value="${currency}">${currency}</option>`;
        });

        // Default: INR to USD
        fromSelect.value = "INR";
        toSelect.value = "USD";
      });

    function convert() {
      const amount = parseFloat(document.getElementById("amount").value);
      const from = document.getElementById("from").value;
      const to = document.getElementById("to").value;

      if (isNaN(amount)) {
        alert("⚠️ Please enter a valid number");
        return;
      }

      // Convert via INR (base currency)
      const inINR = amount * rates[from];
      const converted = inINR / rates[to];

      document.getElementById("result").innerText =
        `Converted Amount: ${converted.toFixed(2)} ${to}`;
    }
  </script>
</body>
</html>
